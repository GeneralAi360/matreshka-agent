interface:
  display_name: "Orchestrate Subagent Work"
  short_description: "Plan, delegate, review, and verify coding work"
  default_prompt: "Use $orchestrating-subagent-work to run this coding task end to end with a bounded autonomy mode and execution profile."
policy:
  allow_implicit_invocation: true
