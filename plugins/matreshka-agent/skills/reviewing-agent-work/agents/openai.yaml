interface:
  display_name: "Matreshka Agent · Review Agent Work"
  short_description: "Matreshka Agent: review scoped work with actionable findings"
  default_prompt: "Use $reviewing-agent-work to perform a read-only review of this scoped change and return consolidated findings."
policy:
  allow_implicit_invocation: true
