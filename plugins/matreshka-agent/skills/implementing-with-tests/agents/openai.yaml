interface:
  display_name: "Implement With Tests"
  short_description: "Implement scoped changes through focused test cycles"
  default_prompt: "Use $implementing-with-tests to implement this bounded change through focused RED and GREEN evidence."
policy:
  allow_implicit_invocation: true
