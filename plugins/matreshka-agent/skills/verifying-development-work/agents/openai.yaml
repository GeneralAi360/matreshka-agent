interface:
  display_name: "Verify Development Work"
  short_description: "Verify development claims with fresh evidence"
  default_prompt: "Use $verifying-development-work to verify these development claims with fresh, scoped evidence."
policy:
  allow_implicit_invocation: true
